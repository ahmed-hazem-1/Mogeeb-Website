[build]
  command = "npm run build"
  publish = "out"

# We're using the static export approach, so we don't need the Next.js plugin
# Remove the environment variables that were causing conflicts

# Handle client-side routing for Next.js
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200